import 'package:flutter/material.dart';

import '../../shared/styles/styles.dart';
class Setting extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Setting',style: taskText(),),
    );
  }
}
